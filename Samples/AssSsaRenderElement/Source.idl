namespace AssSsaRenderElement
{
    [default_interface]
    runtimeclass AssSsaRenderer
    {
        AssSsaRenderer(Windows.UI.Xaml.Controls.SwapChainPanel swapChainPannel, FFmpegInteropX.FFmpegMediaSource mediaSource, Windows.Media.Playback.MediaPlaybackSession playbackSession);

        void Play();

        void Pause();

        void SetFrameUpdateInterval(Windows.Foundation.TimeSpan interval);

        void SetSelectedSubtitle(FFmpegInteropX.SubtitleStreamInfo subtitle);
    }
}
